=====================================================
Aviso de cancelación de tutoría
======================================================

Estimado(a) usuario: <%= @candidate.user_name %>
(<%= @candidate.email %>), se le notifica que el usuario:
<%= @tutor.user_name %> (<%= @tutor.email %>) ha dejado de
ser su tutor(a). Por su comprensión, gracias.

======================================================