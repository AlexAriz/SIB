<%- model_class = University -%>

<br><br><br><br>
<div class="panel-group">
  <div class="panel panel-default">
    <div class="panel-heading"><h1><%=t '.title', :default => model_class
        .model_name.human.titleize %>:<%= @university.name %></h1></div>
    <div class="panel-body">
      <p>
        <strong><%= model_class.human_attribute_name(:name) %>:</strong>
        <%= @university.name %>
      </p>

      <p>
        <strong><%= model_class.human_attribute_name(:city) %>:</strong>
        <%= @university.city %>
      </p>

      <p>
        <strong><%= model_class.human_attribute_name(:state) %>:</strong>
        <%= @university.state %>
      </p>

      <p>
        <strong><%= model_class.human_attribute_name(:country) %>:</strong>
        <%= @university.country %>
      </p>

    </div>
  </div>
  <div class="panel panel-default">
    <div class="panel-heading"><strong><%= model_class.human_attribute_name(:information) %>:</strong></div>
    <div class="panel-body">
      <p>
        <%= @university.information %>
      </p>
      <p>
        <%= link_to 'Página principal de la universidad', @university.url, target: '_blank' %>
      </p>
    </div>
  </div>
</div>

<hr>
<% if can? :edit, @university %>
    <%= link_to 'Editar universidad', edit_university_path(@university), class: 'btn btn-info' %>
<% end %>
<%= link_to 'Regresar', universities_path, class: 'btn btn-danger' %>
<hr>
