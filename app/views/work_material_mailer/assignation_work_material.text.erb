=====================================================
Aviso de cancelación de tutoría
======================================================

Estimado(a) usuario: <%= @candidate.user_name %>
(<%= @candidate.email %>), se le notifica que el tutor:
<%= @tutor.user_name %> (<%= @tutor.email %>) le ha asignado
un material de trabajo.

======================================================